import PagePlaceholder from '../components/layout/PagePlaceholder';

export default function ModulePage() {
  return (
    <PagePlaceholder
      titel="Modulübersicht"
      beschreibung="Alle Module mit Status, Note, ECTS und Prüfungsform."
      phase="Phase 2"
    />
  );
}
