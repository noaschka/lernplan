import PagePlaceholder from '../components/layout/PagePlaceholder';

export default function LernplanPage() {
  return (
    <PagePlaceholder
      titel="Lernplan"
      beschreibung="Themen-Checkliste mit Fortschritt, verknüpft mit dem Prüfungstermin."
      phase="Phase 3"
    />
  );
}
