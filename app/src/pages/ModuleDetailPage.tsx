import PagePlaceholder from '../components/layout/PagePlaceholder';

export default function ModuleDetailPage() {
  return (
    <PagePlaceholder
      titel="Moduldetail"
      beschreibung="Inhalte, Lehrziele, Prüfungsform, SWS, Workload und Voraussetzungen aus dem Modulhandbuch."
      phase="Phase 2"
    />
  );
}
